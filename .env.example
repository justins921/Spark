# Neon Postgres connection string. Created for you when you add a Neon database
# from the Vercel dashboard Storage tab. Pull it locally with `vercel env pull .env.local`.
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# The single password that unlocks the whole app.
APP_PASSWORD="change-me"
